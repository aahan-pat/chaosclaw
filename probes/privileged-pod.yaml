apiVersion: v1
kind: Pod
metadata:
  name: privileged-probe
  namespace: chaosclaw
spec:
  hostPID: true
  containers:
  - name: probe
    image: busybox
    command: ["sleep", "3600"]
    securityContext:
      privileged: true
