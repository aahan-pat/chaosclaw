apiVersion: v1
kind: Pod
metadata:
  name: hostpath-pod
spec:
  containers:
    - name: app
      image: docker.io/library/busybox:1.36
      volumeMounts:
        - name: host-vol
          mountPath: /host-etc
  volumes:
    - name: host-vol
      hostPath:
        path: /etc
